# Vercel installs the serverless function's Python deps from THIS root manifest.
# Production/runtime ONLY — a mirror of backend/requirements.txt with the two
# dev/server-only deps dropped so the function bundle stays small and cold
# starts stay fast:
#   - uvicorn[standard]: a local ASGI server; on Vercel the platform serves the
#     ASGI `app` itself, so it is never imported at runtime.
#   - pytest: tests only.
# Three deps are bumped past backend/.env-era pins ONLY to get Python-3.14
# wheels (Vercel's uv builder forces 3.14 and ignores .python-version): psycopg
# 3.2.13 (3.2.x patch), pydantic 2.13.4 (2.x, pulls a cp314 pydantic-core), and
# bcrypt 5.0.0 (first release with a cp314 wheel). Pillow 12.2.0 already ships
# one. Verified locally (48 tests + auth hash/verify) before deploying.
fastapi==0.115.6
pydantic==2.13.4
pydantic-settings==2.7.1
email-validator==2.2.0
psycopg[binary,pool]==3.2.13
PyJWT==2.10.1
bcrypt==5.0.0
slowapi==0.1.9
httpx==0.28.1
pillow==12.2.0
python-dotenv==1.0.1
